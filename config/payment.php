<?php

return [
    /*
    |--------------------------------------------------------------------------
    | وضع الاختبار
    |--------------------------------------------------------------------------
    |
    | عند ضبط هذه القيمة على true، سيتم محاكاة عمليات الدفع بدلاً من الاتصال الفعلي
    | ببوابات الدفع. هذا مفيد للتطوير والاختبار.
    |
    */
    'test_mode' => env('PAYMENT_TEST_MODE', true),

    /*
    |--------------------------------------------------------------------------
    | بوابات الدفع المفعلة
    |--------------------------------------------------------------------------
    |
    | قائمة ببوابات الدفع المفعلة التي يمكن للمستخدمين استخدامها.
    |
    */
    'enabled_gateways' => ['stripe', 'paypal', 'local'],

    /*
    |--------------------------------------------------------------------------
    | إعدادات Stripe
    |--------------------------------------------------------------------------
    |
    | إعدادات بوابة دفع Stripe.
    |
    */
    'stripe' => [
        'public_key' => env('STRIPE_PUBLIC_KEY', ''),
        'secret_key' => env('STRIPE_SECRET_KEY', ''),
        'webhook_secret' => env('STRIPE_WEBHOOK_SECRET', ''),
    ],

    /*
    |--------------------------------------------------------------------------
    | إعدادات PayPal
    |--------------------------------------------------------------------------
    |
    | إعدادات بوابة دفع PayPal.
    |
    */
    'paypal' => [
        'client_id' => env('PAYPAL_CLIENT_ID', ''),
        'client_secret' => env('PAYPAL_CLIENT_SECRET', ''),
        'sandbox' => env('PAYPAL_SANDBOX', true),
    ],
];
