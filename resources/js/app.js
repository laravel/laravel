import './bootstrap';
import Alpine from 'alpinejs';
import { createApp } from 'vue'

window.Alpine = Alpine;
Alpine.start();

// If using Vue, initialize your app:
// const app = createApp({})
// app.mount('#app')
