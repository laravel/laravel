<x-layouts.app :title="$title"><section class="panel"><div class="panel-head"><h2>{{$title}}</h2></div><div class="table-wrap"><table><thead><tr>@foreach($columns as $column)<th>{{str_replace(['.','_'],' ',ucfirst($column))}}</th>@endforeach</tr></thead><tbody>@forelse($rows as $row)<tr>@foreach($columns as $column)<td>@php($value=data_get($row,$column))@if($value instanceof BackedEnum){{str_replace('_',' ',$value->value)}}@elseif($value instanceof DateTimeInterface){{$value->format('d M Y H:i')}}@elseif(is_bool($value))<span class="badge">{{$value?'Active':'Inactive'}}</span>@else{{$value}}@endif</td>@endforeach</tr>@empty<tr><td colspan="{{count($columns)}}">No records found.</td></tr>@endforelse</tbody></table></div>{{$rows->withQueryString()->links()}}</section></x-layouts.app>
