<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Reset password — MealFlow</title>@vite(['resources/css/app.css'])</head><body class="login"><section class="login-card"><div class="brand">MealFlow<span>Meal delivery operations</span></div><h1>Forgot your password?</h1><p>Enter your account email and we'll send a reset link.</p>@if(session('status'))<div class="success">{{ session('status') }}</div>@endif@if($errors->any())<div class="error">{{ $errors->first() }}</div>@endif<form method="post" action="/forgot-password">@csrf<label>Email<input type="email" name="email" value="{{ old('email') }}" required autofocus></label><button>Send reset link</button></form><p><a href="/login">Back to sign in</a></p></section></body></html>
